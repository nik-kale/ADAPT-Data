global:
  scrape_interval: 5s
  evaluation_interval: 15s

scrape_configs:
  # Scrapes `adapt-data serve <dataset> --port 9091` running on the host.
  - job_name: adapt-data
    static_configs:
      - targets:
          - host.docker.internal:9091
        labels:
          source: adapt-data
